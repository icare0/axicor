use bevy::prelude::*;

pub struct NodeEditorPlugin;

impl Plugin for NodeEditorPlugin {
    fn build(&self, _app: &mut App) {
        // Здесь в будущем будут системы обработки графа нейронов
    }
}
